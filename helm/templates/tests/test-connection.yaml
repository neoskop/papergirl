apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "papergirl.fullname" . }}-test-connection"
  labels:
{{ include "papergirl.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "papergirl.fullname" . }}:8080']
  restartPolicy: Never
